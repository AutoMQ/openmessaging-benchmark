[Unit]
Description=Placement Driver
After=network.target

[Service]
ExecStart=/usr/local/bin/pd --config ./pd-config.yaml
WorkingDirectory={{ pd_dir }}
RestartSec=1s
Restart=on-failure
Type=simple
LimitNOFILE=300000

[Install]
WantedBy=multi-user.target
